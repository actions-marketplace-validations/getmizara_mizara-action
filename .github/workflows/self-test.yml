name: Self-test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Mizara Safety Test on the example policy
        uses: ./
        with:
          policy-path: examples/policy.json
